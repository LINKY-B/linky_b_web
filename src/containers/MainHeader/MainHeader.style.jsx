import styled from "styled-components";
import { Icon } from "components/Icon/index";

export const FilterIcon = styled(Icon.FilterIcon)`
  cursor: pointer;
`;
export const SearchIcon = styled(Icon.SearchIcon)`
  cursor: pointer;
`;
